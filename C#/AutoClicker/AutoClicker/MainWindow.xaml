<Window x:Class="AutoClicker.MainWindow"
        x:Name="mainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:core="clr-namespace:System;assembly=mscorlib"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:autoclicker="clr-namespace:AutoClicker"
        xmlns:enums="clr-namespace:Enums"
        mc:Ignorable="d"
        Title="Auto Clicker" Height="500" Width="600" Background="White">

    <Window.Resources>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <autoclicker:ValueToEqualsParameterConverter x:Key="TextBoxConverter"/>
        <autoclicker:ComboBoxConverter x:Key="ComboBoxConverter"/>
        <ObjectDataProvider x:Key="Types" MethodName="GetValues" ObjectType="{x:Type core:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type Type="enums:InstructionType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="Keys" MethodName="GetValues" ObjectType="{x:Type core:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type Type="enums:VirtualKeyCode"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="ButtonTypes" MethodName="GetValues" ObjectType="{x:Type core:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type Type="enums:ButtonType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="MovementTypes" MethodName="GetValues" ObjectType="{x:Type core:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type Type="enums:MovementType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>

    <DockPanel LastChildFill="True">

        <Grid DockPanel.Dock="Top">
            <Rectangle Height="0.5" Fill="Black"/>
        </Grid>
        <Menu DockPanel.Dock="Top" Padding="0 2" >
            <MenuItem Header="_File">
                <MenuItem Header="_Open" Click="Open_Click"/>
                <MenuItem Header="_Save" Click="Save_Click"/>
                <Separator/>
                <MenuItem Header="_Close" Click="Close_Click"/>
            </MenuItem>

            <MenuItem Header="_Add Instruction">
                <MenuItem Header="_Wait" Click="AddMenu_Wait"/>
                <MenuItem Header="_Loop" Click="AddMenu_Loop"/>
                <Separator/>
                <MenuItem Header="_Hover" Click="AddMenu_Hover"/>
                <MenuItem Header="_Click" Click="AddMenu_Click"/>
                <MenuItem Header="_Drag" Click="AddMenu_Drag"/>
                <MenuItem Header="_Wheel" Click="AddMenu_Wheel"/>
                <Separator/>
                <MenuItem Header="_Keystroke" Click="AddMenu_Keystroke"/>
                <MenuItem Header="_Text" Click="AddMenu_Text"/>
                <Separator/>
                <MenuItem Header="_Clear" Click="Clear_Click"/>
                <!--<MenuItem Header="_" Click="AddMenu"/>-->
            </MenuItem>

            <!--<Separator/>-->
            <MenuItem Header="_Default Values">
                <MenuItem Header="_Delay">
                    <Grid Width="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Minimum" Grid.Column="0"/>
                        <TextBox Margin="10 0 0 0" Padding="1" HorizontalAlignment="Stretch" Grid.Column="1" Text="{Binding Path=GlobalData.Delay, Mode=TwoWay}" />
                    </Grid>
                    <Grid Width="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Random" Grid.Column="0"/>
                        <TextBox Margin="10 0 0 0" Padding="1" HorizontalAlignment="Stretch" Grid.Column="10" Text="{Binding Path=GlobalData.RandomDelay, Mode=TwoWay}"/>
                    </Grid>
                </MenuItem>
                <MenuItem Header="_Repetitions">
                    <Grid Width="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Minimum" Grid.Column="0"/>
                        <TextBox Margin="10 0 0 0" Padding="1" HorizontalAlignment="Stretch" Grid.Column="1" DataContext="{Binding GlobalData, FallbackValue='error'}" Text="{Binding Path=Repetitions, Mode=TwoWay}"/>
                    </Grid>
                    <Grid Width="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Random" Grid.Column="0"/>
                        <TextBox Margin="10 0 0 0" Padding="1" HorizontalAlignment="Stretch" Grid.Column="10" Text="{Binding Path=GlobalData.RandomRepetitions, Mode=TwoWay}"/>
                    </Grid>
                </MenuItem>
                <MenuItem Header="_Speed">
                    <Grid Width="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Minimum" Grid.Column="0"/>
                        <TextBox Margin="10 0 0 0" Padding="1" HorizontalAlignment="Stretch" Grid.Column="1" Text="{Binding Path=GlobalData.Speed, Mode=TwoWay, ElementName=mainWindow}"/>
                    </Grid>
                    <Grid Width="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Random" Grid.Column="0"/>
                        <TextBox Margin="10 0 0 0" Padding="1" HorizontalAlignment="Stretch" Grid.Column="10" Text="{Binding Path=GlobalData.RandomSpeed, Mode=TwoWay}"/>
                    </Grid>
                </MenuItem>
                <Separator/>
                <MenuItem Header="Movement Type">
                    <ComboBox ItemsSource="{Binding Source={StaticResource MovementTypes}}" Width="100"
                              SelectedItem="{Binding Path=GlobalData.MovementType, Mode=TwoWay}">
                    </ComboBox>
                </MenuItem>
                <MenuItem Header="_Position">
                    <Grid Width="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="X Radius" Grid.Column="0"/>
                        <TextBox Margin="10 0 0 0" Padding="1" HorizontalAlignment="Stretch" Grid.Column="1" Text="{Binding Path=GlobalData.RandomX, Mode=TwoWay}"/>
                    </Grid>
                    <Grid Width="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Y Radius" Grid.Column="0"/>
                        <TextBox Margin="10 0 0 0" Padding="1" HorizontalAlignment="Stretch" Grid.Column="10" Text="{Binding Path=GlobalData.RandomY, Mode=TwoWay}"/>
                    </Grid>
                </MenuItem>
                <MenuItem Header="_Drag">
                    <Grid Width="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="X Radius" Grid.Column="0"/>
                        <TextBox Margin="10 0 0 0" Padding="1" HorizontalAlignment="Stretch" Grid.Column="1" Text="{Binding Path=GlobalData.RandomDragX, Mode=TwoWay}"/>
                    </Grid>
                    <Grid Width="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Y Radius" Grid.Column="0"/>
                        <TextBox Margin="10 0 0 0" Padding="1" HorizontalAlignment="Stretch" Grid.Column="10" Text="{Binding Path=GlobalData.RandomDragY, Mode=TwoWay}"/>
                    </Grid>
                </MenuItem>
                <MenuItem Header="_Wheel">
                    <Grid Width="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Minimum" Grid.Column="0"/>
                        <TextBox Margin="10 0 0 0" Padding="1" HorizontalAlignment="Stretch" Grid.Column="1" Text="{Binding Path=GlobalData.Wheel, Mode=TwoWay}"/>
                    </Grid>
                    <Grid Width="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Random" Grid.Column="0"/>
                        <TextBox Margin="10 0 0 0" Padding="1" HorizontalAlignment="Stretch" Grid.Column="10" Text="{Binding Path=GlobalData.RandomWheel, Mode=TwoWay}"/>
                    </Grid>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Hotkeys">
                    <Grid Width="100">
                        <TextBlock Text="Shift" Grid.Column="0" Grid.ColumnSpan="2"/>
                        <ToggleButton Margin="10,0,0,0" Padding="1" Width="15" HorizontalAlignment="Right" Grid.Column="1" IsChecked="{Binding Path=GlobalData.Shift, Mode=TwoWay}"/>
                    </Grid>
                    <Grid Width="100">
                        <TextBlock Text="Ctrl" Grid.Column="0" Grid.ColumnSpan="2"/>
                        <ToggleButton Margin="10,0,0,0" Padding="1" Width="15" HorizontalAlignment="Right" Grid.Column="1" IsChecked="{Binding Path=GlobalData.Ctrl, Mode=TwoWay}"/>
                    </Grid>
                    <Grid Width="100">
                        <TextBlock Text="Alt" Grid.Column="0" Grid.ColumnSpan="2"/>
                        <ToggleButton Margin="10,0,0,0" Padding="1" Width="15" HorizontalAlignment="Right" Grid.Column="1" IsChecked="{Binding Path=GlobalData.Alt, Mode=TwoWay}"/>
                    </Grid>
                </MenuItem>
            </MenuItem>

        </Menu>
        <Grid DockPanel.Dock="Top">
            <Rectangle Height="0.5" Fill="Black"/>
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="0.5*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                <ToggleButton x:Name="PlayButton" BorderThickness="0" Background="Transparent" IsChecked="{Binding ElementName=mainWindow, Path=IsPlaying, Mode=TwoWay}" Padding="2" Margin="5 0" Width="35" Height="35">
                    <Image Source="Images/play.png" RenderOptions.BitmapScalingMode="HighQuality">
                        <Image.ToolTip>
                            <ToolTip>
                                <TextBlock Text="Start playing the script."/>
                            </ToolTip>
                        </Image.ToolTip>
                    </Image>
                    <ToggleButton.ContextMenu>
                        <ContextMenu>
                            <Grid Width="120">
                                <TextBlock Text="Minimize on start" Grid.Column="0" Grid.ColumnSpan="2"/>
                                <ToggleButton Margin="10,0,0,0" Padding="1" Width="15" HorizontalAlignment="Right" Grid.Column="1" IsChecked="{Binding Path=MinimizeOnPlay, Mode=TwoWay}"/>
                            </Grid>
                            <Grid Width="120">
                                <TextBlock Text="Maximize on stop" Grid.Column="0" Grid.ColumnSpan="2"/>
                                <ToggleButton Margin="10,0,0,0" Padding="1" Width="15" HorizontalAlignment="Right" Grid.Column="1" IsChecked="{Binding Path=MaximizeOnStopPlay, Mode=TwoWay}"/>
                            </Grid>
                        </ContextMenu>
                    </ToggleButton.ContextMenu>
                </ToggleButton>
                <ToggleButton x:Name="RecordButton" BorderThickness="0" Background="Transparent" IsChecked="{Binding ElementName=mainWindow, Path=IsRecording, Mode=TwoWay}" Padding="2" Margin="5 0" Width="35" Height="35">
                    <Image Source="Images/record2.png" RenderOptions.BitmapScalingMode="HighQuality">
                        <Image.ToolTip>
                            <ToolTip>
                                <TextBlock Text="Start recording some instructions."/>
                            </ToolTip>
                        </Image.ToolTip>
                    </Image>
                    <ToggleButton.ContextMenu>
                        <ContextMenu>
                            <Grid Width="120">
                                <TextBlock Text="Minimize on start" Grid.Column="0" Grid.ColumnSpan="2"/>
                                <ToggleButton Margin="10,0,0,0" Padding="1" Width="15" HorizontalAlignment="Right" Grid.Column="1" IsChecked="{Binding Path=MinimizeOnRecord, Mode=TwoWay}"/>
                            </Grid>
                            <Grid Width="120">
                                <TextBlock Text="Maximize on stop" Grid.Column="0" Grid.ColumnSpan="2"/>
                                <ToggleButton Margin="10,0,0,0" Padding="1" Width="15" HorizontalAlignment="Right" Grid.Column="1" IsChecked="{Binding Path=MaximizeOnStopRecord, Mode=TwoWay}"/>
                            </Grid>
                        </ContextMenu>
                    </ToggleButton.ContextMenu>
                </ToggleButton>
                <ToggleButton x:Name="WithDelayButton" BorderThickness="0" Background="Transparent" IsChecked="{Binding ElementName=mainWindow, Path=IsRecordingWithDelay, Mode=TwoWay}" Padding="2" Margin="5 0" Width="35" Height="35">
                    <Image Source="Images/stopwatch2.png" RenderOptions.BitmapScalingMode="HighQuality">
                        <Image.ToolTip>
                            <ToolTip>
                                <TextBlock Text="Enable delay measurement between instructions."/>
                            </ToolTip>
                        </Image.ToolTip>
                    </Image>
                </ToggleButton>
            </StackPanel>

            <TextBlock Foreground="Black" FontSize="16" Grid.Column="1" Text="Auto Clicker" FontWeight="Bold" Margin="0" VerticalAlignment="Center" HorizontalAlignment="Center"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBox x:Name="RepetitionsTextBox" IsEnabled="False" TextChanged="RepetitionsTextBox_TextChanged" Text="5" MaxLength="5" VerticalAlignment="Center" Width="40" TextAlignment="Right" Padding="2"/>

                <ToggleButton x:Name="InfiniteButton" BorderThickness="0" Background="Transparent" IsChecked="{Binding Path=IsInfiniteLooping, Mode=TwoWay}" Padding="2" Margin="5 0" Width="35" Height="35">
                    <Image Source="Images/infinity.png" RenderOptions.BitmapScalingMode="HighQuality">
                        <Image.ToolTip>
                            <ToolTip>
                                <TextBlock Text="Enable infinite looping."/>
                            </ToolTip>
                        </Image.ToolTip>
                    </Image>
                </ToggleButton>
            </StackPanel>
        </Grid>

        <RichTextBox DockPanel.Dock="Top"
                     Margin="10 0 10 10"
                     Padding="5"
                     x:Name="InstructionsTextBox"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     TextChanged="InstructionsTextBox_TextChanged"
                     PreviewKeyDown="InstructionsTextBox_PreviewKeyDown"
                     VerticalScrollBarVisibility="Auto">
            
            <RichTextBox.Resources>
                <Style TargetType="{x:Type Paragraph}">
                    <Setter Property="Margin" Value="0"/>
                </Style>
            </RichTextBox.Resources>
        </RichTextBox>

    </DockPanel>
</Window>